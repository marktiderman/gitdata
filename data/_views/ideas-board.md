<!-- DERIVED VIEW — generated by `gitdata rollup` from data/ideas/*.md.
     Do not hand-edit: `gitdata rollup --check` will fail if you do. -->
# Ideas

**7 ideas** · 6 open · 1 became work · 0 declined

Good ideas nobody is building yet, and the record of the ones we decided against. `solves` is the
field that earns a row: an idea that cannot name the problem it removes is a preference.

## Open

| id | title | solves | raised |
| --- | --- | --- | --- |
| I-001 | Declare column types in the projection instead of discarding them | The projection knows every value's type at load time and throws it away, so every comparison against a column is a chance to reint | Session 2026-08-07, delivering the 0.2.1 `where:` fix (PR #15) — the root cause found whil |
| I-002 | A gate that checked nothing should not exit 0 | `validate` and `rollup --check` both exit 0 over a store with no schemas and no views, so a required CI check stays green while re | Session 2026-08-07 — carried in from the doctor design (ruling R6) and MEASURED on a scrat |
| I-003 | Ship type declarations, so the contract we tell consumers to import is typed | The package exports `isRowFile`/`rowFilesIn` and tells consumers to import them instead of reimplementing the row contract — but s | Session 2026-08-07 — hit as a real CI failure in a downstream consumer (gamify-platform `C |
| I-004 | Assertions — a query that must return zero rows, or the build fails | gitdata proves a rendered artifact matches its rows. That is self-consistency, and it is green over wrong data by construction. No | Session 2026-08-07 — asked by the operator after PR #15 landed: what would make gitdata 10 |
| I-005 | Nothing in a generated artifact says which engine produced it | Determinism is the product, but the engine version is an undeclared input to it. A board regenerated by a different version can di | Session 2026-08-07 — observed in a consumer carrying SEVEN hand-typed copies of the versio |
| I-007 | Let a schema declare which files in its directory are rows | A table cannot hold anything but rows. Putting a per-record attachment beside its record — a flightlog next to a task — breaks the | Session 2026-08-07, Gamify (GamifyEducation/gamify-platform). Wanted flightlogs at `data/t |

## Became work

| id | title | became | solves |
| --- | --- | --- | --- |
| I-006 | `{field: {not: null}}` is not the complement of `{field: null}` | #16 | `{parent: null}` and `{parent: {not: null}}` should partition a table and instead overlap on three of five row |

## Declined

| id | title | verdict |
| --- | --- | --- |
| — | nothing declined | — |
